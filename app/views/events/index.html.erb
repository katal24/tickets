<h1> Wszystkie wydarzenia</h1>
<ul>
    <% for event in @events %>
        <li>
            <%= event.artist %>
             <%= link_to "Show", "/events/#{event.id}" %>
             </li>
    <% end %>
</ul>

<%= link_to 'Nowe ogłoszenie', events_new_path %>
<%= render 'tickets/menu' %>